// import { fromJS } from 'immutable';
// import { makeSelectMessagePageDomain } from '../selectors';

// const selector = makeSelectMessagePageDomain();

describe('makeSelectMessagePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
