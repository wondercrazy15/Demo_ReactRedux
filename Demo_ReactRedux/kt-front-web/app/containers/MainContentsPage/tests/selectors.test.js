// import { fromJS } from 'immutable';
// import { makeSelectMainContentsPageDomain } from '../selectors';

// const selector = makeSelectMainContentsPageDomain();

describe('makeSelectMainContentsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
